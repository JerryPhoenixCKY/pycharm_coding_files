# set() 是 Python 中用于创建集合（set）的内置函数。集合是一种无序、不重复、可变的数据结构，常用于去重、成员检测、集合运算等场景。
#
# 下面详细解释 set() 的不同用法：
#
# ✅ 1. 创建空集合
# python
# s = set()
# print(s) # 输出: set()
# print(type(s)) # <class 'set'>
# ⚠️ 注意：不能用 {} 创建空集合，因为 {} 表示空字典！
#
# ✅ 2. 从可迭代对象创建集合（自动去重）
# 🔸 从列表创建
# python
# s = set([1, 2, 2, 3, 4, 4])
# print(s) # {1, 2, 3, 4}
# 🔸 从字符串创建（每个字符作为元素）
# python
# s = set("hello")
# print(s) # {'h', 'e', 'l', 'o'} # 注意：'l' 只出现一次
# 🔸 从元组创建
# python
# s = set((1, 1, 2, 3))
# print(s) # {1, 2, 3}
# ✅ 任何可迭代对象（list, tuple, str, range 等）都可以传给 set()。
#
# ✅ 3. 集合的基本操作
# 🔸 添加元素：.add()
# python
# s = set()
# s.add(1)
# s.add(2)
# s.add(2) # 无效，不会重复
# print(s) # {1, 2}
# 🔸 删除元素
# .remove(x)：如果 x 不存在，会报错
# .discard(x)：如果 x 不存在，不报错
#
# python
# s = {1, 2, 3}
# s.remove(2) # 删除 2
# s.discard(5) # 安全删除，5 不存在也不报错
# print(s) # {1, 3}
# 🔸 清空集合
# python
# s.clear() # 变成空集合
#
# ✅ 4. 集合的数学运算（重点！）
#
# 假设有两个集合：
# python
# A = {1, 2, 3}
# B = {3, 4, 5}
#
# 运算 代码 结果 说明
# ------ ------ ------ ------
# 并集 A \ B 或 A.union(B) {1, 2, 3, 4, 5} 所有在 A 或 B 中的元素
# 交集 A & B 或 A.intersection(B) {3} 同时在 A 和 B 中的元素
# 差集 A - B 或 A.difference(B) {1, 2} 在 A 中但不在 B 中的元素
# 对称差集 A ^ B 或 A.symmetric_difference(B) {1, 2, 4, 5} 在 A 或 B 中，但不同时在两者中的元素
# 示例：
# python
# A = {1, 2, 3}
# B = {3, 4, 5}
# print(A B) # {1, 2, 3, 4, 5}
# print(A & B) # {3}
# print(A - B) # {1, 2}
# print(A ^ B) # {1, 2, 4, 5}
#
# ✅ 5. 成员检测（非常快！）
#
# python
# s = {10, 20, 30}
# print(20 in s) # True
# print(40 in s) # False
# 集合的 in 操作平均时间复杂度是 O(1)，比列表（O(n)）快得多！
#
# ✅ 6. 实际应用场景
# 🔸 场景 1：去重
# python
# nums = [1, 2, 2, 3, 3, 3]
# unique_nums = list(set(nums))
# print(unique_nums) # [1, 2, 3]（顺序可能变）
# 🔸 场景 2：找共同好友
# python
# user1_friends = {"Alice", "Bob", "Charlie"}
# user2_friends = {"Bob", "David", "Eve"}
# common = user1_friends & user2_friends
# print(common) # {'Bob'}
# 🔸 场景 3：过滤无效数据
# python
# valid_ids = {101, 102, 103}
# input_ids = [101, 999, 102, 888]
# filtered = [x for x in input_ids if x in valid_ids]
# print(filtered) # [101, 102]
#
# ⚠️ 注意事项
#
# 1. 集合中的元素必须是不可变类型（如 int, str, tuple），不能是 list 或 dict。
# python
# s = {[1, 2]} # ❌ 报错！列表不可哈希
# s = {(1, 2)} # ✅ 元组可以
#
# 2. 集合是无序的，不能通过索引访问：
# python
# s = {1, 2, 3}
# print(s[0]) # ❌ 报错！
#
# 3. 如果需要有序且去重，可以用 dict.fromkeys() 或 collections.OrderedDict（Python 3.7+ 字典默认有序）：
# python
# nums = [3, 1, 2, 2, 1]
# unique_ordered = list(dict.fromkeys(nums))
# print(unique_ordered) # [3, 1, 2]
#
# ✅ 总结：set() 的主要用途
#
# 用途 示例
# ------ ------
# 创建空集合 s = set()
# 去重 set([1,1,2]) → {1,2}
# 快速查找 if x in my_set:
# 集合运算 A & B, A \ B 等
# 记录唯一事件 如记录“哪些用户登录过”
#
# 集合是 Python 中非常高效且实用的数据结构，尤其适合处理唯一性和成员关系问题！
