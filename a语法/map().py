# map() 是 Python 中一个非常常用的内置函数，用于对一个可迭代对象（如列表、元组等）中的每个元素应用某个函数，并返回一个 map 对象（在 Python 3 中是迭代器）。
#
# 🔹 基本语法：
# map(function, iterable)
# function：要应用的函数（可以是内置函数、自定义函数、lambda 表达式等）。
# iterable：可迭代对象，比如列表、元组、字符串等。
#
# ✅ 示例 1：将字符串列表转为整数列表
# str_list = ['1', '2', '3', '4']
# int_list = list(map(int, str_list))
# print(int_list) # 输出: [1, 2, 3, 4]
# 这里 int 是函数，str_list 是可迭代对象。map(int, str_list) 把每个字符串转成整数。
#
# ✅ 示例 2：用 lambda 表达式对每个元素平方
# numbers = [1, 2, 3, 4]
# squares = list(map(lambda x: x 2, numbers))
# print(squares) # 输出: [1, 4, 9, 16]
#
# ✅ 示例 3：多个可迭代对象（较少用但有用）
#
# map 也支持多个 iterable，这时函数要能接收对应数量的参数：
# a = [1, 2, 3]
# b = [10, 20, 30]
# result = list(map(lambda x, y: x + y, a, b))
# print(result) # 输出: [11, 22, 33]
# 注意：当多个 iterable 长度不同时，map 会在最短的那个结束时停止。
#
# ✅ 常见用途：配合 input().split()
#
# 你经常看到这样的代码：
#
# l, m = map(int, input().split())
#
# 解释：
# 1. input() 读入一行字符串，比如 "500 3"
# 2. .split() 拆分成列表：['500', '3']
# 3. map(int, ...) 把每个字符串转成整数 → 相当于 int('500'), int('3')
# 4. 然后用多重赋值赋给 l 和 m
#
# 等价于：
# python
# parts = input().split()
# l = int(parts[0])
# m = int(parts[1])
#
# 但 map 写法更简洁！
#
# ⚠️ 注意：
# map() 返回的是 迭代器*（不是列表），所以通常要用 list() 包裹才能看到全部结果。
# 如果你只是遍历一次，可以直接用 for x in map(...):，不用转成列表。
#
# ✅ 总结
#
# 用法 说明
# ------ ------
# map(int, ['1','2']) 转整数
# map(str.upper, ['a','b']) 转大写
# map(lambda x: x2, [1,2]) 自定义操作
# l, m = map(int, input().split()) 读取多个整数输入（最常见！）
#
# 如有具体场景，可以告诉我，我帮你写例子 😊
